// Using the while loop
        let number = 3;
        let counts ="";
// Exceute until 500
        while (number != 500) {
            number = number += 1;
            counts = counts+ number +  "\n";

        }      
        console.log(counts);